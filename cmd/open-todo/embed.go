package main

//go:generate go run github.com/kevinburke/go-bindata/go-bindata -o bindata.go -pkg main -prefix "../../web/dist" ../../web/dist/...

// Embed will be expanded in later tasks to serve the frontend build.
